export type Snack = {
  id: string;
  brand: string;
  product: string;
  description: string;
  image: string;
  votes: number;
}

export type FutureSnack = {
  id: string;
  brand: string;
  product: string;
  votes: number;
}